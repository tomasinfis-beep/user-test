# UNDO V11

A fast MVP for AI transaction-recovery intake, internal triage, analysis, notifications and success-fee tracking.

The public form is intentionally simple and uses Supabase directly so the intake path stays resilient. Internal actions use Vercel server functions with server-side secrets.
